import React from 'react';

interface NotificationSection {

}
const NotificationSection: React.FC<NotificationSection> = ({ }) => {
  return (
    <NotificationSection></NotificationSection>
  );
};
export default NotificationSection;
